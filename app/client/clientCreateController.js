'use strict';

(function () {
    angular.module('myApp')
        .controller('clientCreateController', clientCreateController);

    clientCreateController.$inject = ['$scope'];
    function clientCreateController($scope) {

    }
})();